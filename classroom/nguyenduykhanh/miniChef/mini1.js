function max(x, y) {
    
    var result = "";
    if (isNaN(x) || isNaN(y)) {
        console.log("wrong type, x or y must be a number");       
        } else {
           return result = Math.max(x,y);
        }  
}
