function max(x, y, z) {
    
    var result = "";
    if (isNaN(x) || isNaN(y) || isNaN(z)) {
        console.log("wrong type, x or y or z must be a number");       
        } else {
           return result = Math.max(x,y,z);
        }  
}
